/* eslint-disable no-undef */
import getSKUs from './getThis';

export default async function () {
    // eslint-disable-next-line no-undef
    //alert(await baseTest());
    alert(await getSKUs());
    console.log(await getSKUs());
}
